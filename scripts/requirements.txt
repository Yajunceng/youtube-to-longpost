# 已验证可用(2026-06-09 实测跑通)
youtube-transcript-api>=1.0.0   # 抓 YouTube 字幕
yt-dlp>=2025.10.14              # 下载音频/视频(下限放宽:yt-dlp 按月发版,钉死最新版会导致部分环境 pip 拉不到、安装失败)
faster-whisper>=1.0.0            # 无字幕时音频转写
imagehash>=4.3.1                 # 帧去重(感知哈希)— 已验证
Pillow>=10.0.0                   # 图像处理 — 已验证

# 多模态读画面(抽帧已验证,描述待接入 API key)
# 多模态:anthropic 或 openai,二选一,跑前装
# anthropic>=0.40.0
# openai>=1.50.0

# 系统依赖(非 pip):ffmpeg —— macOS 用 `brew install ffmpeg`(已验证 8.1.1)
